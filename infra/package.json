{
  "name": "@mfp/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK infrastructure - ECS Fargate + ALB (API), RDS PostgreSQL, S3 + CloudFront (SPA)",
  "bin": {
    "mfp-infra": "bin/app.ts"
  },
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "bootstrap": "cdk bootstrap",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "aws-cdk": "^2.170.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
